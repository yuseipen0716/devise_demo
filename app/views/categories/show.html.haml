-# ここはカテゴリーに紐づいた記事を一覧表示するページ
%body
  .container
    %h2
      = @category.name
    .row.g-2
    - @categorizations.each do |categorization|
      .card.bg-light.my-4
        .card-body.p-4
          .small.text-muted
            = "created_at: #{categorization.article.created_at.strftime("%Y/ %m/ %d")} | updated_at: #{categorization.article.created_at.strftime("%Y/ %m/ %d")}"
          %h5.card-title.mt-3
            = categorization.article.title
          %p.card-text
            = categorization.article.content.truncate(50, omission: '...<続きは下のボタンから>')
          .d-flex.justify-content-end
            = button_to "記事の続き", categorization.article, :method => :get, class: "btn btn-primary mx-3" 
            - if admin_signed_in?
              = button_to "編集", edit_article_path(categorization.article), :method => :get, class: "btn btn-success mx-3"
              = button_to "削除", categorization.article, :method => :delete, :data => { :confirm => '本当に削除しますか?' }, class: "btn btn-danger mx-3"
