.container.w-75
  -# 記事詳細画面
  %h1.fw-bolder
    = @article.title
  .container
    .small.text-muted
      = "created_at: #{@article.created_at.strftime("%Y/ %m/ %d")} | updated_at: #{@article.created_at.strftime("%Y/ %m/ %d")}"
